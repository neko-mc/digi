{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${mod_version}",
  "name": "${mod_name}",
  "description": "${mod_desc}",
  "authors": [
    "Atakku"
  ],
  "contact": {
    "homepage": "https://mc.neko.rs/${mod_id}"
  },
  "license": "${license}",
  "icon": "assets/${mod_id}/icon.png",
  "environment": "*",
	"entrypoints": {
		"main": [
			"dev.atakku.digi.store.DigiStore"
		]
	},
  "depends": {
    "java": "${dep_java}",
    "minecraft": "${dep_mc}",
    "fabricloader": "${dep_fl}"
  }
}
