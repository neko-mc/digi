{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "name": "${mod_name}",
  "version": "${mod_version}",
  "environment": "*",
  "license": "${mod_license}",
  "icon": "assets/${mod_id}/icon.png",
  "contact": {
    "homepage": "${link_home}/${mod_id}",
    "issues": "${link_repo}/issues",
    "sources": "${link_repo}"
  },
  "authors": [
    "Atakku"
  ],
  "depends": {
    "java": "${deps_java}",
    "minecraft": "${deps_minecraft}",
    "fabricloader": "${deps_loader}"
  },
  "description": "${mod_desc}",
  "custom": {
    "modmenu": {
      "links": {
        "modmenu.discord": "${link_discord}"
      }
    }
  }
}